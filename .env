# ==================================================
# Search-2api 配置文件 (.env)
# ==================================================

# 1. search.sh 的 Cookie
# 这是项目运行的【绝对核心】，请务必从浏览器开发者工具中获取并粘贴到下面。
# 获取方法请参考上面的教程。
SEARCH_SH_COOKIE="test"

# ⚠️ 特别注意：如果您的 Cookie 值中包含美元符号($)，必须将每一个 `$` 都替换成两个 `$$`。
# 例如：原始值为 `...Cc$oyk...`，必须改为 `...Cc$$oyk...`
# 这是为了防止 Docker 将其误认为变量。

# 2. API 访问密钥
# 这是您调用 API 时需要提供的密码，可以自定义为您喜欢的任何字符串。
# 在请求的 Header 中需要加入 "Authorization: Bearer your_secret_api_key_here"
API_MASTER_KEY="xxx"

# 3. Nginx 代理端口
# 这是您的服务对外暴露的端口，默认为 8080。
# 您可以通过 http://localhost:8080 访问服务。
# NGINX_PORT=8080

# 4. FastAPI 应用端口 (通常无需修改)
# 这是内部 FastAPI 应用运行的端口，Nginx 会将请求转发到这里。
# PORT=8000
